---
title: Быстрый старт
description: Самый короткий путь от пустого проекта к проверенному prompt для oh-my-agent с ожидаемыми результатами и шагами восстановления.
---

# Быстрый старт

Используйте эту страницу, чтобы проверить работу harness до чтения полного справочника. Вам понадобятся каталог проекта и хотя бы один поддерживаемый AI CLI или IDE. Установщик может настроить `bun`, `uv`, Serena и CUE в macOS, Linux или Windows; для первого prompt требуется выбранная интеграция host, а интеграции provider и browser необязательны.

## 1. Установите harness проекта

Из каталога проекта запустите bootstrap-установщик:

```bash
curl -fsSL https://raw.githubusercontent.com/first-fluke/oh-my-agent/main/cli/install.sh | bash
```

В Windows PowerShell выполните:

```powershell
irm https://raw.githubusercontent.com/first-fluke/oh-my-agent/main/cli/install.ps1 | iex
```

Интерактивная настройка запрашивает язык ответов, CLI-вендоров, провайдеров возможностей, preset модели, preset навыков проекта и вариант стека. Для первого запуска оставьте значения по умолчанию, выберите уже используемого вендора и preset проекта, наиболее близкий к репозиторию.

Если `bun` уже установлен, используйте установщик напрямую:

```bash
bunx oh-my-agent@latest
```

Bootstrap-скрипты устанавливают OMA в текущий проект. Используйте `oma install --global`, когда нужен уровень HOME; перед смешиванием проектной и глобальной установки прочитайте [Установку](./installation.md).

## 2. Проверьте результат

Запустите проверку состояния из того же каталога проекта:

```bash
oma doctor
```

Успешный результат означает, что интеграция выбранного вендора и файлы `.agents/` готовы. Необязательные интеграции MCP, browser, memory или code intelligence могут появиться как предупреждения; они нужны только для задач, которые их используют. Для просмотра разрешённых моделей и CLI каждой канонической роли агента используйте `oma doctor --profile`.

Если команда не найдена, CLI установлен вне текущего `PATH`; откройте новый shell или добавьте каталог bin менеджера пакетов. Если `oma doctor` сообщает о недопустимой конфигурации, исправьте указанное поле и запустите проверку снова. Не удаляйте `.agents/oma-config.yaml` для восстановления: это принадлежащая пользователю конфигурация, сохраняющая настройки между обновлениями.

## 3. Выполните одну небольшую задачу

Откройте репозиторий в настроенном AI-инструменте и опишите одно самостоятельное изменение:

```text
Add a validation message to the existing email field. Follow the project's current form and test conventions. Done when the invalid-email case is covered by a focused test.
```

Если для выбранного host включён keyword hook, он может активировать подходящий workflow. Маршрутизацию навыков выполняет host или выбранный workflow, поэтому произвольный prompt host не гарантирует hook, конкретный skill или `CHARTER_CHECK`. Контракт выполнения всё равно должен проверить соглашения репозитория, ограничиться указанной областью и сообщить о проверке. Точные файлы и команда зависят от проекта; приведённый prompt служит примером.

Для задачи, пересекающей границы API и UI, явно выберите `/work` или `/orchestrate`. Для одной предметной области перейдите к [выполнению одного навыка](../guide/single-skill.md). Более длинные примеры находятся в [руководстве по использованию](../guide/usage.md).

## 4. Узнайте значения по умолчанию до масштабирования

OMA запускается с `model_preset: auto`, Serena для code intelligence, Agent Memory для semantic memory, native web search и отключённой telemetry. Serena использует общий transport `bridge` и автоматически обновляется, если это не переопределено. Browser DevTools MCP включается явно; новая интерактивная установка сначала предлагает Aside. Последствия и ключи переопределения описаны в разделе [Важные значения по умолчанию](./important-defaults.md).

Если управляемая задача остановилась, начните с `oma agent status <session-id> [agent-id]`, затем проверьте её receipt в `.agents/state/agent-runs/` и внедрённый путь structured claim. Эти записи показывают run, task, workspace, exit code и статус проверки. Читаемые человеком файлы `result-*.md` и `progress-*.md` в `.agents/state/memories/` добавляют контекст, если они существуют. Повторяйте только наименьшую неудачную команду после того, как убедитесь, что run больше не активен. Persistent workflow остаётся активным до завершения или команды `workflow done`; восстановление по state-файлу описано в [рабочих процессах](../core-concepts/workflows.md#persistent-mode-mechanics).

## Следующие шаги

- [Важные значения по умолчанию](./important-defaults.md): приоритеты, provider и варианты восстановления
- [Установка](./installation.md): preset, настройка vendor, глобальная установка и обновления
- [Агенты](../core-concepts/agents.md): 33 пакета навыков и роли dispatch
- [Рабочие процессы](../core-concepts/workflows.md): планирование, параллельное выполнение, QA и persistent mode
